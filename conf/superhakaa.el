; スーパーハカー()っぽい演出

;(setq split-height-threshold nil)
;(setq split-width-threshold 0)

;; (defun other-window-or-split ()
;;   (interactive)
;;   (when (one-window-p)
;;     (split-window-horizontally))
;;   (other-window 1))
;; (global-set-key (kbd "C-e") 'other-window-or-split)
